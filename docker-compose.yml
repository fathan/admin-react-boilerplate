version: "3.8"

services:
  web:
    build: .
    ports:
      - "3000:80"   # localhost:3000 -> Nginx:80
    restart: always
    depends_on:
      - backend

  backend:
    image: your-backend-image
    ports:
      - "8000:8000"
    environment:
      - NODE_ENV=development